<nav class="navbar navbar-expand-lg navbar-dark" style="background-color: #5C8FFF;">
    <a class="navbar-brand" href="#">Nameless !</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-link">
                <strong>
                    {% if is_granted('ROLE_USER') %}
                        Welcome {{ app.user.name }} !
                    {% endif %}
                </strong>
            </li>
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('home_index') }}">Home<span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item">
                {% if is_granted('IS_AUTHENTICATED_FULLY') == false %}
                    <a class="nav-link" href="{{ path('app_register') }}">Sign up</a>
                {% endif %}
            </li>
            {% if is_granted('ROLE_USER') %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('patient_index') }}">Personal Space</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('totem_show', {user: app.user.id}) }}">My DoctoCat</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('badges_show', {id: app.user.id}) }}">My Badges</a>
                </li>
            {% endif %}
            <li class="nav-item">
                {% if app.user %}
                    <a class="nav-link" href="{{ path('app_logout') }}">Logout</a>
                {% else %}
                    <a class="nav-link" href="{{ path('app_login') }}">Login</a>
                {% endif %}
            </li>

        </ul>
    </div>
</nav>
