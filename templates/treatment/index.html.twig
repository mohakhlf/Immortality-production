{% extends 'base.html.twig' %}

{% block title %}Treatment index{% endblock %}

{% block body %}
    <h1>Treatment index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for treatment in treatments %}
            <tr>
                <td>{{ treatment.id }}</td>
                <td>{{ treatment.name }}</td>
                <td>
                    <a href="{{ path('treatment_show', {'id': treatment.id}) }}">show</a>
                    <a href="{{ path('treatment_edit', {'id': treatment.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('treatment_new', {id: app.user.id}) }}">Create new</a>
{% endblock %}
